# Railway/Nixpacks configuration
# Forces Node.js detection (prevents "Detected Staticfile" error)

[phases.setup]
nixPkgs = ["nodejs_20", "npm"]

[phases.install]
cmds = ["npm ci || npm install"]

[start]
cmd = "node server.js"
